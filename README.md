# This repo is contain my example of SOLID principle.
> I used Java to write an abstraction of these example not the whole application.
> Each principle contain two examples: the violation and the solution.